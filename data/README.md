# Data

This folder contains the provided competition files only.

- `input/train.csv`: training data with the target column `Drafted`
- `input/test.csv`: rows to predict for submission
- `input/sample_submission.csv`: required submission format

Competition rules prohibit external data and hand-labeling. Keep generated predictions in `output/`.
